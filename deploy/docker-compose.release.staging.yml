# STAGING override — port 8080 only, Traefik disabled, zero public-host claim.
# Usage: OUTBOUND_IMAGE=seoscanaudit/outbound:phase1-<COMMIT_SHA> \
#        docker compose -f deploy/docker-compose.release.yml \
#                       -f deploy/docker-compose.release.staging.yml up -d
services:
  seo-scan:
    ports:
      - "${HOST_PORT:-8080}:8000"
    labels: []
